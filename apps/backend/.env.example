# Option A: Local MongoDB
DATABASE_URL="mongodb://localhost:27017/fullstack_users"

# Option B: MongoDB Atlas (replace placeholders)
# DATABASE_URL="mongodb+srv://USER:PASSWORD@HOST/fullstack_users?retryWrites=true&w=majority"

PORT=3000

# CORS origin for production (comma-separated for multiple origins)
# CORS_ORIGIN="https://your-frontend.vercel.app,https://your-domain.com"
